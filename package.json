{
  "name": "web-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build-and-export": "npm run clean-build && next build && next export",
    "build-and-run": "npm run clean-build && next build && next export && next start -p 8080",
    "install-dependencies": "rm -rf ./node_modules && npm i",
    "clean-build": "rm -rf .next && rm -rf out",
    "start": "next start"
  },
  "dependencies": {
    "@types/react-form": "^4.0.2",
    "antd": "^4.12.3",
    "dotenv": "^16.0.0",
    "next": "12.0.2",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-form": "^4.0.1",
    "sass": "^1.49.8"
  },
  "devDependencies": {
    "@types/react": "17.0.20",
    "autoprefixer": "^10.3.4",
    "postcss": "^8.3.6",
    "tailwindcss": "^2.2.15",
    "typescript": "4.4.3"
  }
}
